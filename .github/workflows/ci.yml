name: CI

on:
  push:
    branches: [main]
  pull_request:

# Žádný job nic nezapisuje zpět do repa (jen checkout + build/test) — GITHUB_TOKEN proto
# jen ke čtení, viz CodeQL "missing-workflow-permissions".
permissions:
  contents: read

jobs:
  backend:
    name: Backend (Spring Boot)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5.7.0
        with:
          distribution: temurin
          java-version: '25'
      - uses: gradle/actions/setup-gradle@v6
      # `build` spouští i testy (unit, bez DB — spring-boot-docker-compose je jen
      # developmentOnly a žádný test nestartuje Spring context) a ověří, že se Liquibase
      # changelogy parsují.
      - run: ./gradlew build

  frontend:
    name: Frontend (Angular)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm test -- --watch=false
      - run: npm run build

  mobile:
    name: Mobile (Android)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5.7.0
        with:
          distribution: temurin
          java-version: '21'
      - uses: android-actions/setup-android@v4
      - uses: gradle/actions/setup-gradle@v6
      - run: ./gradlew :app:assembleDebug
