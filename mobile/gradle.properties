org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# JDK 25 (systémový výchozí) je s AGP 9.2.1 zatím míň prověřený než JDK 21 — bezpečnější volba
# jen pro tenhle modul, backend na Javě 25 běží bez problémů (viz backend/build.gradle). Volba
# JDK je přenositelná přes Gradle toolchain (viz app/build.gradle.kts), ne přes absolutní cestu
# tady — ta by na jiném stroji (i v CI) build hned na startu shodila.
